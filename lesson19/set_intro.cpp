// Lesson 19 - 26.11.2020

#include <iostream>
#include <set>

using namespace std;

int main() {
    set<int> s;
    s.insert(10);
    s.insert(1);
    s.insert(5);
    s.insert(3);
    s.insert(8);
    s.insert(9);
    

    for (int i = 0; i < n; i++){
        
    }

    for(int i=0 ; i<n ; i++){

    }
    
    
    
    cout<<(*s.begin());
    
    return 0;
}